#include<iostream>
using namespace std;

int main()
{
    int n, key;
    cin>>n>>key;
    
    int arr[1000];
    
    for(int i=0;i<ni++)
    {
      if(a[i] == key)
      {
          cout<<value<<" ";
          break;
      }
    }
    
    if(i==n)
      cout<<"key not found";
  return 0;
  
 } 
